name : CI

on: [push]

jobs: 
    build:
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4
            - name: Run one-line script
              run: echo "Hello, world"
            - name: Print secret
              run: echo ${{ secrets.TEST }}
